using System;

namespace Fractal {
    public static class Constants {
        public static readonly float HW_RATIO = (float)(Math.Sqrt(3.0)) / 2.0f;
        public static readonly int KOCH_ITERS_DEFAULT = 5;
    }
}
